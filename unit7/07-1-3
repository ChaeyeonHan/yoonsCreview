#include<stdio.h>

int main(void) {
	int num, sum = 0;
	printf("정수를 입력하세요: ");
	scanf_s("%d", &num);
	while (num != 0)
	{
		sum = sum + num;
		printf("정수를 입력하세요 : ");
		scanf_s("%d", &num);
	}
	printf("합계는 %d입니다.", sum);
	return 0;
}
